apiVersion: apps/v1
kind: Deployment
metadata:
  name: pqc-secure-transfer
spec:
  template:
    spec:
      containers:
      - name: pqc-secure-transfer
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"